# E2E Test Environment - Disables Supabase to use LocalStorageAdapter
# This file is loaded when running Vite with --mode test
# Used by: playwright.config.ts (npm run dev -- --mode test)

# Disable Supabase to force LocalStorageAdapter for test data seeding
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
